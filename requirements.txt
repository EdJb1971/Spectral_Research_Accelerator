# SpectralEarth Scientific Backend Requirements
# Optimized for Python 3.9+ and Pydantic v1 compatibility

# Web API Framework
fastapi>=0.100.0,<0.111.0
uvicorn>=0.22.0

# Data Validation (Compatible with Pydantic v1 validators used in main.py)
pydantic>=1.10.0,<2.0.0

# Relational Database Mapper
sqlalchemy>=2.0.0

# Deep Learning and Tensor Core
# PyPI may resolve this to a CPU-only wheel. Install the appropriate official CUDA or ROCm
# wheel first when accelerator execution is required; README.md records the verified command.
torch>=2.0.0,<3.0.0

# Scientific & Grid Computing
numpy>=1.24.0,<3.0.0
pandas>=2.0.0
xarray>=2023.5.0

# Testing and Mock Validation
pytest>=7.3.0
httpx>=0.24.0

# Schema migrations (T3.5.8). Not optional and not deferrable: `create_all` never adds a
# column to an existing table, which had already left spectral_earth.db unqueryable (D32).
alembic>=1.13.0

# Numerical utilities: Hungarian assignment (4D tracking), k-NN for the KSG
# mutual-information estimator (E8), signal helpers
scipy>=1.11.0

# Notebook dashboard (research_playground.ipynb) - previously undeclared (D10)
ipywidgets>=8.0.0
plotly>=5.18.0
matplotlib>=3.7.0

# Test-only reference oracles for validating our own transforms (R8/T3.5.6).
# Not imported by src/ - used to cross-check coefficients against a known-good
# implementation rather than trusting our round-trip tests alone.
PyWavelets>=1.4.0

# Cloud-native ERA5 over Zarr (T3.5.18). `xarray.open_zarr` needs zarr; `dask` gives the
# lazy chunked reads that make "fetch only the chunks the crop touches" true rather than
# aspirational; `gcsfs` is the driver for the gs:// WeatherBench 2 buckets (public, no
# credentials). Network access is still opt-in via SPECTRALEARTH_ALLOW_NETWORK.
zarr>=2.16,<3.0
dask[array]>=2023.5.0
fsspec>=2023.5.0
gcsfs>=2023.5.0

# A NetCDF4/HDF5 engine, which was MISSING (defect D33). `xarray` was declared but no
# engine was, and data/README.md invited the researcher to drop an ERA5 `.nc` file into
# data/. Modern ERA5 downloads are NetCDF4/HDF5, so that path failed with "found the
# following matches ... but their dependencies may not be installed" - a documented feature
# that could not work. scipy alone reads only NetCDF3 classic.
h5netcdf>=1.1.0
h5py>=3.8.0

# Multipart form parsing, required by FastAPI's UploadFile (T3.5.24). Without it the import
# endpoints raise at import time, not at request time - a startup failure rather than a 500.
python-multipart>=0.0.6
